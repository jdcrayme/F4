// f4-renderer/src/coord_transform_dummy.cpp
//
// coord_transform.hpp is header-only (all functions are inline).
// CMake requires at least one .cpp file per static library target,
// so this file exists as a placeholder.
