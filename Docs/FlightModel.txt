Let me explore the FreeFalcon repository to find and analyze the flight model source code.
Let me start by exploring the repository structure and then diving into the flight model source files.
Now let me fetch the critical flight model source files. I'll read them in parallel batches.
Let me now fetch the core implementation files in parallel to build the complete picture.
Now let me fetch the remaining critical files in parallel.
......
Now let me get the last few key files to complete the picture.......
I've now read and analyzed all 15+ core flight model source files. Here's the comprehensive breakdown:

---

# Falcon 4 / FreeFalcon Flight Model — Detailed Architecture Breakdown

## 1. Overall Architecture

The flight model lives in `src/sim/airframe/` and is implemented as the `AirframeClass`. It follows a **fixed-order pipeline** executed every simulation frame via `AirframeClass::Exec()`, which runs **multiple sub-steps per major frame** (`SimLibMinorPerMajor` iterations) for numerical stability:

```
for (i = 0; i < SimLibMinorPerMajor; i++) {
    Atmosphere()           ← ISA atmosphere + airspeed
    FlightControlSystem()  ← Gain scheduling + Pitch/Roll/Yaw/Axial
    Aerodynamics()         ← CL, CD, CY lookups + force resolution
    EngineModel(dt)        ← Thrust + fuel + RPM spool
    EquationsOfMotion(dt)  ← Quaternion kinematics + position integration
}
RunLandingGear()           ← Visual gear animation (once per major frame)
```

The model has **two modes**: a full 6-DOF model for the player aircraft and a "SuperSimple" model for AI/distant aircraft that bypasses the FCS and directly commands alpha/Nz.

---

## 2. Atmosphere Model (`atmos.cpp`)

Implements a **3-layer ISA (International Standard Atmosphere)**:

| Layer | Altitude Range | Temperature Ratio (θ/θ₀) | Density Ratio (ρ/ρ₀) |
|-------|---------------|--------------------------|---------------------|
| Troposphere | 0–36,089 ft | `1 − 6.875×10⁻⁶ × h` | `(θ/θ₀)^4.2559` |
| Lower Stratosphere | 36,089–65,617 ft | 0.751865 (constant) | `0.297076 × e^(0.00004806 × (36089−h))` |
| Upper Stratosphere | >65,617 ft | `0.682457 + h/945374` | `(0.978261 + h/659515)^(−35.16)` |

From these, it derives:
- **Speed of sound:** `a = √θ × a_SL` (where `a_SL = 1116.45 ft/s`)
- **Air density:** `ρ = σ × ρ_SL`
- **Dynamic pressure:** `q̄ = ½ρV²`
- **Normalized dynamic pressure:** `q_som = q̄ × S / m` (used throughout the FCS)
- **Mach number** from `vt / a`
- **Calibrated airspeed (CAS)** using the isentropic pitot tube equation (separate subsonic/supersonic formulas), with a **Newton-Raphson** inverse solver (`CalcMach`) for converting KIAS back to Mach.

---

## 3. Aerodynamic Model (`aero.cpp`)

### 3.1 Coefficient Lookups

All aerodynamic coefficients are stored as **2D lookup tables** indexed by (Mach, α) and loaded from `.dat` files per aircraft type. The core function uses `Math.TwodInterp()` for bilinear interpolation:

```
CL  ← aeroData->clift[Mach × Alpha] × clFactor
CD  ← aeroData->cdrag[Mach × Alpha] × cdFactor
CY  ← aeroData->cy[Mach × Alpha]    × cyFactor
```

### 3.2 High-Lift Device Effects

**TEF (Trailing Edge Flaps)** and **LEF (Leading Edge Flaps)** modify the effective angle of attack:

```
tempAlpha = α + tefFactor − lefFactor
```

- `tefFactor` and `lefFactor` are normalized deflections (current angle / max angle)
- TEFs increase effective AoA (more camber → more lift), LEFs decrease it
- The final CL is scaled: `CL × (1 + tefFactor × CL_tefFactor)`
- CD is similarly modified by both TEF and LEF factors

### 3.3 Lift Curve Slope (Numerical Differentiation)

The local lift curve slope `CL_α` and normal force slope `CN_α` are computed via **central difference** at ±2°:

```
CL_α  = (CL(α+2°) − CL(α−2°)) × 0.25 × TEF_factor
CN_α  = ((CL(α+2°)−CL(α−2°))cos(α) + (CD(α+2°)−CD(α−2°))sin(α)) × 0.25
```

These derivatives are **critical** — they drive the pitch FCS gains, trim AoA, and stall detection.

### 3.4 Ground Effect

Within one wingspan of the ground, lift is boosted up to **13%** at 20% span height, linearly fading to zero at full span height. Disabled for AI aircraft.

### 3.5 Stores Drag

External stores (pylons, tanks, missiles) add drag via the Limiter system: `CD_stores = Limiter(mach) × dragIndex`.

### 3.6 Stall Model

Uses the classic formula: `V_stall = 17.16 × √((W/S) / |CL|)`

When `V_cas < V_stall` or `α > α_critical`:
- Lift is severely reduced: `L = min(0, CL × 0.5) × (V_cas / V_stall)`
- An automatic roll command is applied: `p_cmd = sin(φ) × 80° × DTR`
- This models the asymmetric lift loss and wing drop at stall

### 3.7 Force Resolution

Aerodynamic forces are computed in **three coordinate systems**:

```
Body axis:      xaero = −D·cos(α) + L·sin(α)
                yaero = CY·q̄·S·(β − |β|·y_shape·0.5)
                zaero = −L·cos(α) − D·sin(α)

Stability axis: xsaero = −D,  zsaero = −L
Wind axis:      xwaero = xsaero·cos(β) + ysaero·sin(β)
```

---

## 4. Flight Control System (FCS)

### 4.1 Top-Level Dispatcher (`fcs.cpp`)

`FlightControlSystem()` runs in this order:
1. **Damper scheduling** — pitch/yaw and roll damping factors via Q̄-dependent limiter tables
2. **`Gains()`** — compute all control law gains (see §4.4)
3. **`SetStallConditions()`** — evaluate stall state machine
4. **`Pitch()`**, **`Roll()`**, **`Yaw()`**, **`Axial()`** — axis control laws

### 4.2 Gain Scheduling (`gain.cpp`)

The heart of the FCS. Computes **time-varying gains** based on current flight conditions:

**AOA Bias (1G trim):**
```
α_bias = [g·cos(γ)·cos(μ) / q_som + 0.1·gear − CL₀·TEF_factor] / CL_α,0 − tefFactor + lefFactor
```

**Command Mode Selection:**
- If `α_max × CL_α × q_som / g > max_G` → **G-command mode** (Nz control)
- Otherwise → **AOA-command mode** (direct alpha control)

**Pitch Axis — Second-Order Control Law:**
- Computes a "Fitch theta-2" time constant: `t_θ₂ = V / (g × Nz_α)` (Nz per radian alpha)
- Solves a quadratic characteristic equation for the closed-loop poles, yielding two time constants `tp02`, `tp03`
- Forward path gain: `kp05 = tp02 × tp03 × ω²` (or with the `g/(q̄·S·CN_α)` factor in Nz mode)
- Landing gear modifies gains via `pitchGearGain`

**Roll Axis:**
- Time constant `tr01` is Q̄-scheduled (0.416s at 100 psf → 0.25s at 250+ psf)
- Max roll rate from 2D table: `kr01 = TwodInterp(α, q̄, rollTable) × DTR`
- Coupling term: `kr02 = cos(α)`

**Yaw Axis:**
- Damping ratio `zy01 = 0.70`, bandwidth coupled to roll: `ωy = 0.3 / tr01`
- Same quadratic pole-placement structure as pitch

### 4.3 Pitch Control (`pitch.cpp`)

Two paths based on command mode:

**AOA Command Mode** (F-16 style — stick position → desired alpha):
```
max_α_cmd = α + (G_max − CL·q_som/g − cos(μ)·cos(γ)) × g / (q_som × CN_α)
```
Chain of limiters: Cat III AOA → Positive G → AOA → Negative G

**Nz Command Mode** (stick → desired G-load):
```
error = (p_stick × G_available − (Nz − cos(μ)·cos(γ) − gear_correction)) × kp05
```

**Both modes use a PI controller with Adams-Bashforth integration:**
```
e_prop = kp02 × error
e_intg = FIAdamsBash(kp03 × error, dt, history)   ← 4-point Adams-Bashforth
α_cmd = e_prop + e_intg
```

**Alpha integration** uses a **7-term Tustin (bilinear) integrator** for numerical stability:
```
α = F7Tust(α_cmd, τ_pitch × pitchMomentum, dt, history)
```

**Stall recovery overrides** force specific alpha commands: ±60° in deep stall with oscillating timers, blended recovery toward saved trim state.

### 4.4 Roll Control (`roll.cpp`)

**Rate-command system** with alpha-dependent limiting:

```
p_cmd = r_stick × kr01                          ← stick → rate command
p_cmd *= RollRateLimiter(α − α_error)           ← alpha-dependent table
p_cmd *= min(V_cas / V_switch, 1.0)             ← speed reduction below 220 KCAS
p_cmd × 0.5 if gear down                         ← gear reduction
```

The alpha-error term couples body rates into the limiter: `α_error = (|q/2| + |p|) × 0.1 × RTD`, effectively reducing the available roll rate at high AoA when the aircraft is already rotating.

**Closed-loop response** via first-order lag:
```
p_stable = FLTust(p_cmd, tr01 × rollMomentum, dt, history)
```

**Roll angle limiting** drives the command back toward `maxRoll` if exceeded. **Asymmetric thrust** from twin engines adds: `(thrust_L − thrust_R) × 0.05`.

### 4.5 Yaw Control (`yaw.cpp`)

**PI controller on lateral acceleration error:**

```
Ny_cmd = y_stick × 2.0                          ← rudder pedals → Ny command (±2g max)
Ny_cmd ×= min(q_som × β_max × 0.05 / g, 1.0)  ← Q̄ availability scaling
Ny_cmd ×= AlphaLimiter(α)                       ← alpha-dependent
Ny_cmd ×= RollRateLimiter(|p|)                  ← roll-rate limiting
Ny_cmd ×= 5.0 / Limiter(V_cas × 0.8)           ← speed normalization
Ny_cmd ×= Limiter((350 − V_cas) × 0.2)          ← high-speed yaw damping

error = (Ny_cmd + Ny_cg_offset) × ky05
β_cmd = clamp(ky02 × error + AdamsBash(ky03 × error), β_min, β_max) × yl_sdamp
```

Anti-windup clamps the integrator and resets history when beta limits are hit.

**Beta integration** is a first-order lag: `β = FLTust(β_cmd, ty02 × yawMomentum, dt, history)`.

**Asymmetric thrust** adds: `(thrust_R − thrust_L) × 1.5` to the beta command.

### 4.6 Axial Control (`axial.cpp`)

Manages **actuator dynamics** for speed brake, gear, and tail hook (simple first-order position models: `pos += 0.3 × dt × cmd`).

**Thrust effectiveness** computation:
```
nozzle_area = 1.0 − throttle_reverse
effective_thrust = nozzle_area − throttle_reverse × 0.8666
```

**Over-speed gear damage**: if `q̄ > maxQ̄` (300–350 psf), a probabilistic damage model triggers gear structural failure.

---

## 5. Engine Model (`engine.cpp`)

### 5.1 Thrust Computation

Thrust is **2D-interpolated** from (Altitude × Mach) tables for three power settings:

```
Thrust = TwodInterp(alt, mach, table[power_setting])
```

| Throttle Position | Power Setting | Table Index |
|---|---|---|
| 0.0 (Idle) | Idle | `[0]` |
| 1.0 (MIL) | Military | `[1]` |
| 1.0+ (AB) | Afterburner | `[2]` |

**Interpolation:**
- MIL power: `T = ((T_MIL − T_idle) × PL + T_idle) / mass`
- Afterburner: `T = (2 × (T_AB − T_MIL) × (PL − 1.0) + T_MIL) / mass`

### 5.2 RPM Spool Model

First-order lag with altitude/Mach correction:
```
RPM_cmd = 0.7 + 0.3 × PL (MIL)  or  1.0 + 0.06 × (PL − 1.0) (AB)
spool_rate_adj = spool_rate + (−alt/25000) + (−mach/2)
RPM = FLTust(RPM_cmd, spool_rate_adj, dt, history)
```

**Engine states:** Stopped → Off → Running, with JFS start sequences, windmilling RPM (`KIAS/450 × 0.12`), and flameout handling.

### 5.3 Engine-Specific RPM Corrections

Different engines have unique RPM schedules:
- **PW-100/220**: Idle RPM ramps with Mach (0.84–1.4M), altitude adds `(alt/10000)/30 + 0.7`
- **PW-229/GE-129**: Reduced speed excursion zone at 0.55–1.1M, probabilistic AB lighting
- **F-14 TF30**: RPM spools to 0.85 when `α > 18°` and `M < 0.9` (stall prevention)

### 5.4 Fuel System

Multi-tank fuel model with priority-based consumption, tank-to-tank transfer rates, external drop tank jettison, and fuel state tracking (internal vs. external). Fuel flow is either table-interpolated or proportional to thrust.

---

## 6. Equations of Motion (`eom.cpp`)

### 6.1 Orientation Update

**Quaternion kinematics** — the orientation is stored as a quaternion (e1, e2, e3, e4) and updated each timestep via `CalcBodyRates(dt)` and `CalcBodyOrientation(dt)`, followed by trigonometric extraction of Euler angles (`Trigenometry()`).

### 6.2 Velocity Equation

```
V̇_tangential = x_aero + x_prop − g·sin(γ)
```

The net acceleration drives the true airspeed `vt` via `CalculateVt(dt)`.

### 6.3 Position Integration

**Earth-frame velocities** account for wind:
```
ẋ = vt·cos(γ)·cos(σ) + wind_x
ẏ = vt·cos(γ)·sin(σ) + wind_y
ż = −vt·sin(γ)
```

Position is updated with Euler integration: `x += ẋ × dt`.

### 6.4 Ground Model

**Ground contact** uses spring-damper dynamics with `SetGroundPosition()`:
- Clamps aircraft to terrain surface
- Gear spring compression model
- Ground friction via `CalcMuFric()`: surface-type-dependent (dry/wet/crosswind), brake-state-dependent (parking/analog/digital/wheel brakes), carrier arrest

**Takeoff detection:** triggers when `q̄·CN_α > 0.55` (enough aerodynamic authority) and vertical velocity is positive.

**Landing impact:** discriminated by nose angle and impact angle. Successful landing vs. crash/bounce is determined by threshold comparisons, with ATC notification on success.

### 6.5 Stall State Machine

Defined in `airframe.cpp::SetStallConditions()`:

```
None → EnteringDeepStall → DeepStall → Spinning → FlatSpin
                                       ↓
                                   Recovering → None
```

Trigger conditions are weight/asymmetry-dependent (e.g., `α > 31° − 9×(W/W_empty − 1.3) − 10×|asymmetry/W|`), meaning loaded aircraft with asymmetric stores stall earlier.

---

## 7. Data Pipeline (`readin.cpp`)

All aircraft-specific data is loaded from `.dat` files at startup:

```
actypes.lst → list of aircraft types
For each type:
  acdata/<name>.dat →
    Scalar params: weight, area, fuel, α/β limits, G-limits, gear geometry, CG, span
    AeroData:     CL[Mach×α], CD[Mach×α], CY[Mach×α] + scaling factors
    EngineData:   Thrust[3×Alt×Mach], FuelFlow[3×Alt×Mach], AB flag
    RollData:     MaxRollRate[α×q̄]
    AuxAeroData:  ~500 fields (spool rates, flap params, fuel tanks, TFR gains, sounds, animations)
    Limiters:     Q̄-dependent limiter tables (AOA, G, roll rate, damping)
```

---

## 8. Simplified Model (`supersimple.cpp`)

AI aircraft use a drastically simplified FCS:
- **Roll:** first-order lag on stick command (`p = 0.75·stick·300° + 0.26·p_prev`)
- **Pitch:** direct `Nz = cos(γ)·cos(φ) + stick·G_available`, then `α = Nz·g / (q_som·CN_α)`
- **Drag reduction:** uses 75% of true α for drag lookup (less speed bleed in dogfights)
- Includes `P_subS` (specific excess power) and `SustainedGs` (max sustained G via iterative α search) for AI energy management

---

## 9. Trim Solver (`trim.cpp`)

Finds steady-state (α, throttle) at startup via **Newton's method** iteration:
- Solves `acc_z = z_aero + z_prop + g → 0` (vertical equilibrium) for α
- Solves `acc_x = x_aero + x_prop → 0` (longitudinal equilibrium) for throttle
- Up to 13 iterations per axis, with speed adjustment if trim fails

---

## 10. Turbulence Model (`turbulence.cpp`)

Multi-source probabilistic turbulence:
- **Mechanical** (low altitude, wind-proportional): amplitude = `wind × 0.1`
- **Thermal** (water/urban, daytime only): up to 5.0 amplitude
- **Cloud convection** (near cumulus cells within 18,400 ft): up to 1.0
- **Tropopause** (FL350–FL360): amplitude 2.0
- Night reduces all amplitudes; poor weather adds continuous stratus turbulence
- Output smoothed by 1st-order low-pass filter (`FLTust`, τ = 1.0s for vertical, 5.0s for roll)

---

## Summary of Key Design Decisions

| Aspect | Approach |
|--------|----------|
| **Integration** | Adams-Bashforth (4-point) for integral terms, Tustin bilinear (7-term) for alpha, first-order lag (FLTust) for rates |
| **Aero data** | 2D tables (Mach × α) with bilinear interpolation; numerically differentiated for stability derivatives |
| **FCS architecture** | Gain-scheduled PI controller (pitch), rate-command with alpha limiter (roll), PI on Ny error (yaw) |
| **Engine** | 2D thrust tables (Alt × Mach) with first-order RPM spool lag |
| **Orientation** | Quaternion representation with Euler angle extraction |
| **Ground** | Spring-damper with surface-dependent friction |
| **Stall** | Weight/asymmetry-dependent state machine with oscillating recovery logic |
| **AI simplification** | Direct α = Nz·g/(q̄·S·CN_α) conversion, 75% drag reduction |
That covers the full flight model architecture from data loading through to state integration. If you'd like, I can dive deeper into any specific subsystem (e.g., the quaternion kinematics, the Adams-Bashforth integrator implementation, or the detailed FCS pole-placement math in `gain.cpp`), or I can produce this as a formatted PDF/docx document for reference.

Recomendations for improvement:
Great question — the FreeFalcon codebase is essentially late-1990s C++ (Watcom C/C++ V10 era), so there's a *lot* to modernize. Let me walk through the most impactful refactorings with concrete before/after examples drawn directly from the code we analyzed.

---

## 1. Stall State Machine: `enum switch` → `std::variant` + `std::visit`

This is the single biggest win. The stall mode is an enum with switches scattered across `pitch.cpp`, `roll.cpp`, `yaw.cpp`, `eom.cpp`, and `airframe.cpp`. Each switch has to know about every state.

**Before** (scattered across ~5 files):
```cpp
// airframe.h
enum StallMode { None, EnteringDeepStall, DeepStall, Spinning, FlatSpin, Recovering };
int stallMode;

// pitch.cpp — one of many switches
switch (stallMode) {
    case EnteringDeepStall: aoacmd = 60.0F*DTR*sign + 5.0F*DTR*pshape; break;
    case DeepStall:         aoacmd = oscTimer*stallMag*(0.3F-fabs(r))*3.3F; break;
    case Spinning:         aoacmd = ±60.0F*DTR based on roll; break;
    case FlatSpin:         aoacmd = 0; break;
    case Recovering:       aoacmd = lerp towards saved trim; break;
    case None:
    default:               /* normal FCS path */; break;
}

// roll.cpp — duplicated switch
switch (stallMode) {
    case DeepStall:         pscmd = sin(phi)*±5*DTR + osc; break;
    case EnteringDeepStall: pscmd = sin(phi)*±40*DTR + rshape*kr01*0.1; break;
    // ...
}
```

**After** — each state is a type with its own behavior:
```cpp
// stall_modes.hpp
struct StallNone {
    float computePitchCommand(float aoacmd, float pshape, float alpha) const {
        return aoacmd;  // pass through — normal FCS
    }
    float computeRollCommand(float pscmd, float phi, float rshape,
                             float kr01, float oscTimer) const {
        return pscmd;  // pass through
    }
    std::optional<StallState> evaluate(float vcas, float alpha, float weight,
                                       float emptyWeight, float asymmetry,
                                       float loadingFraction, VU_TIME time) const;
};

struct DeepStall {
    float magnitude;
    float oscillationTimer;
    float savedPitchState;

    float computePitchCommand(float, float pshape, float r) const {
        return oscillationTimer * magnitude * (0.3F - std::fabs(r)) * 3.3F;
    }
    float computeRollCommand(float, float phi, float, float, float) const {
        return std::sin(phi) * 5.0F * DTR;
    }
};

struct Spinning {
    float direction;  // +1 or -1
    float oscillationTimer;

    float computePitchCommand(float, float, float) const {
        return direction * 60.0F * DTR;
    }
    float computeRollCommand(float, float, float, float, float oscTimer) const {
        return oscTimer * DTR;
    }
};

// ... FlatSpin, Recovering, EnteringDeepStall similarly

using StallState = std::variant<
    StallNone, EnteringDeepStall, DeepStall, Spinning, FlatSpin, Recovering
>;

// Usage in pitch.cpp — no switch needed:
struct PitchVisitor {
    float aoacmd, pshape, alpha, r;

    float operator()(const StallNone& s) const {
        return s.computePitchCommand(aoacmd, pshape, alpha);
    }
    float operator()(const DeepStall& s) const {
        return s.computePitchCommand(aoacmd, pshape, r);
    }
    float operator()(const Spinning& s) const {
        return s.computePitchCommand(aoacmd, pshape, alpha);
    }
    // ... one overload per state
};

aoacmd = std::visit(PitchVisitor{aoacmd, pshape, alpha, r}, stallState_);
```

**Why this matters:** The switch in `pitch.cpp` and `roll.cpp` no longer need to be kept in sync. Adding a new stall mode means adding one new struct + one visitor overload — no touching existing code. And each state encapsulates its own data (no more `oldp02[5]` saved-state hacks).

---

## 2. Engine Model: `if (nEngines == 2)` → Strategy Pattern

The runtime branch between single-engine and multi-engine appears in `airframe.cpp::Exec()` and `engine.cpp`:

**Before:**
```cpp
// airframe.cpp
void AirframeClass::Exec() {
    for (int i = 0; i < SimLibMinorPerMajor; i++) {
        Atmosphere();
        FlightControlSystem();
        Aerodynamics();
        if (nEngines == 2)
            MultiEngineModel(dt);   // 1172 lines, handles thrust1/thrust2/rpm1/rpm2
        else
            EngineModel(dt);        // 678 lines, handles single thrust/rpm
        EquationsOfMotion(dt);
    }
}
```

**After:**
```cpp
// iengine_model.hpp
class IEngineModel {
public:
    virtual ~IEngineModel() = default;

    // Returns net body-axis forces and updates fuel state
    struct EngineOutput {
        float xprop, zprop;   // body axis thrust
        float xsprop, zsprop; // stability axis
        float xwprop;         // wind axis
        float fuelUsed;       // lbs this timestep
        float rpm;            // average or primary RPM
    };
    virtual EngineOutput update(float dt) = 0;

    virtual void setThrottle(float throttle) = 0;
    virtual float getRPM() const = 0;
    virtual bool isRunning() const = 0;
    virtual void startJFS() = 0;
};

// single_engine_model.hpp
class SingleEngineModel : public IEngineModel {
public:
    SingleEngineModel(const EngineData* data, const AuxAeroData* aux,
                      float mass, AircraftClass* platform);

    EngineOutput update(float dt) override;

private:
    const EngineData* data_;
    const AuxAeroData* aux_;
    float rpm_{0.0f};
    float fuelFlow_{0.0f};
    FilterHistory rpmFilter_;  // replaces oldRpm[]
    EngineState state_{EngineState::Stopped};
    // ... no thrust1/thrust2 duplication
};

// multi_engine_model.hpp
class MultiEngineModel : public IEngineModel {
public:
    EngineOutput update(float dt) override;
    // asymmetric thrust is just left_.getRPM() vs right_.getRPM()
private:
    SingleEngineModel left_, right_;  // compose, don't duplicate
};

// airframe.cpp — clean
void AirframeClass::Exec() {
    for (int i = 0; i < SimLibMinorPerMajor; i++) {
        Atmosphere();
        FlightControlSystem();
        Aerodynamics();
        auto engineOut = engineModel_->update(dt);
        xprop = engineOut.xprop;
        // ...
        EquationsOfMotion(dt);
    }
}

// Construction — data-driven
std::unique_ptr<IEngineModel> makeEngineModel(const AeroDataSet& data, ...) {
    if (data.auxaeroData->nEngines == 2)
        return std::make_unique<MultiEngineModel>(...);
    return std::make_unique<SingleEngineModel>(...);
}
```

**Bonus:** `MultiEngineModel` can now compose two `SingleEngineModel` instances instead of duplicating 1172 lines of spool/thrust/fuel logic. The asymmetric thrust yaw/roll corrections in `roll.cpp` and `yaw.cpp` become `engineModel_->getThrustAsymmetry()` instead of reaching into raw `thrust1`/`thrust2` member variables.

---

## 3. Aero Data Tables: Raw C Arrays → Type-Safe `Table2D<T>`

The current code passes around raw `float*` arrays with separate `numMach`/`numAlpha` counts to `Math.TwodInterp()`. This is error-prone (wrong array, wrong size) and gives zero type safety.

**Before:**
```cpp
// airframe.h
class AeroData {
public:
    int numMach;
    int numAlpha;
    float *mach;    // caller must know this has numMach elements
    float *alpha;   // caller must know this has numAlpha elements
    float *clift;   // caller must know this has numMach*numAlpha elements
    float *cdrag;
    float *cy;
    ~AeroData() { delete[] mach; delete[] alpha; delete[] clift; delete[] cdrag; delete[] cy; }
};

// aero.cpp — usage (no bounds checking, no type safety)
cl = Math.TwodInterp(mach, tempAlpha, aeroData->mach, aeroData->alpha,
                     aeroData->clift, aeroData->numMach,
                     aeroData->numAlpha, &curMachBreak, &curAlphaBreak);
```

**After:**
```cpp
// table2d.hpp
template<typename T>
class Table2D {
public:
    Table2D(std::vector<float> rowKeys, std::vector<float> colKeys,
            std::vector<T> values)
        : rowKeys_(std::move(rowKeys))
        , colKeys_(std::move(colKeys))
        , values_(std::move(values))
    {
        assert(rowKeys_.size() * colKeys_.size() == values_.size());
    }

    // Bilinear interpolation with clamping at boundaries
    T interpolate(float rowKey, float colKey) const {
        auto [r0, r1, rf] = findBracket(rowKeys_, rowKey);
        auto [c0, c1, cf] = findBracket(colKeys_, colKey);

        T v00 = at(r0, c0), v10 = at(r1, c0);
        T v01 = at(r0, c1), v11 = at(r1, c1);

        return v00*(1-rf)*(1-cf) + v10*rf*(1-cf)
             + v01*(1-rf)*cf     + v11*rf*cf;
    }

    // Central-difference numerical derivative along column axis
    T dByCol(float rowKey, float colKey, float delta = 2.0f) const {
        return (interpolate(rowKey, colKey + delta)
              - interpolate(rowKey, colKey - delta)) / (2.0f * delta);
    }

    size_t numRows() const { return rowKeys_.size(); }
    size_t numCols() const { return colKeys_.size(); }

private:
    std::vector<float> rowKeys_;  // e.g., Mach breakpoints
    std::vector<float> colKeys_;  // e.g., Alpha breakpoints
    std::vector<T> values_;       // row-major: values_[r * numCols + c]

    T at(size_t r, size_t c) const { return values_[r * colKeys_.size() + c]; }

    struct Bracket { size_t lo, hi; float frac; };
    Bracket findBracket(const std::vector<float>& keys, float key) const {
        auto hi = std::upper_bound(keys.begin(), keys.end(), key);
        if (hi == keys.begin()) return {0, 0, 0.0f};
        if (hi == keys.end())   return {keys.size()-2, keys.size()-1, 1.0f};
        auto lo = std::prev(hi);
        float frac = (key - *lo) / (*hi - *lo);
        return {static_cast<size_t>(lo - keys.begin()),
                static_cast<size_t>(hi - keys.begin()), frac};
    }
};

// aero_data.hpp — clean, no raw pointers
struct AeroData {
    Table2D<float> cl;   // CL(Mach, Alpha)
    Table2D<float> cd;   // CD(Mach, Alpha)
    Table2D<float> cy;   // CY(Mach, Alpha)
    float clFactor{1.0f}, cdFactor{1.0f}, cyFactor{1.0f};

    // Convenience
    float clAlpha(float mach, float alpha) const {
        return cl.dByCol(mach, alpha, 2.0f) * clFactor;
    }
};

// aero.cpp — usage is now self-documenting and bounds-safe
cl = aeroData->cl.interpolate(mach, tempAlpha) * aeroData->clFactor;

// The central-difference in the original aero.cpp becomes:
clalpha = aeroData->cl.dByCol(mach, tempAlpha, 2.0f) * 0.25f
        * (1.0f + tefFactor * auxaeroData->CLtefFactor);
```

The `Math.TwodInterp` calls throughout the codebase (aero, engine, roll, gain, trim, cruise — ~30+ call sites) all collapse to `table.interpolate(x, y)`. No more passing `numMach`, `numAlpha`, `&curMachBreak`, `&curAlphaBreak` everywhere.

---

## 4. The Monolithic Class → Composition

`AirframeClass` currently has ~200 member variables spanning atmosphere, FCS, aero, engine, ground, fuel, gear, and stall. It's 3000+ lines across its implementation files.

**Before** (everything in one class):
```cpp
class AirframeClass {
    // Atmosphere state
    float mach, qbar, vcas, qsom, rho, sound;
    // FCS state (pitch)
    float aoacmd, alpdot, pshape, kp01..kp05, tp01..tp03, eprop, eintg;
    SAVE_ARRAY oldp01, oldp02, oldp03, oldp04, oldp05;
    // FCS state (roll)
    float pscmd, pstab, kr01, kr02, tr01;
    SAVE_ARRAY oldr01;
    // FCS state (yaw)
    float betcmd, beta, ky01..ky03, eprop_y, eintg_y;
    SAVE_ARRAY oldy01, oldy03;
    // Engine
    float rpm, thrust, xprop, zprop, fuelFlowSS, pwrlev;
    // ... 180+ more members
};
```

**After** — each subsystem is its own class:
```cpp
class AtmosphereModel {
    float mach_{0}, qbar_{0}, vcas_{0}, qsom_{0}, rho_{0}, sound_{0};
public:
    void update(float vt, float alt);
    float mach() const { return mach_; }
    float qbar() const { return qbar_; }
    float qsom() const { return qsom_; }
    // ...
};

class FlightControlSystem {
    PitchAxis pitch_;
    RollAxis  roll_;
    YawAxis   yaw_;
    AxialAxis axial_;
    GainScheduler gains_;
public:
    void update(const AtmosphereModel& atmos, const AeroData& aero, float dt);
    const PitchAxis& pitch() const { return pitch_; }
    // ...
};

class Airframe {
    AtmosphereModel   atmos_;
    FlightControlSystem fcs_;
    AeroModel         aero_;
    std::unique_ptr<IEngineModel> engine_;
    EquationsOfMotion eom_;
    StallStateMachine stall_;
    GroundModel       ground_;
    FuelSystem        fuel_;
    GearModel         gear_;

public:
    void exec(float dt) {
        for (int i = 0; i < kMinorPerMajor; ++i) {
            atmos_.update(vt_, alt_);
            fcs_.update(atmos_, *aeroData_, dt);
            aero_.update(atmos_, fcs_, *aeroData_);
            auto eng = engine_->update(dt);
            eom_.update(atmos_, aero_, eng, ground_, dt);
        }
        gear_.update(dt);
    }
};
```

Now `pitch.cpp` only has access to `PitchAxis` members — no more accidentally reading `kr01` from the roll axis or `beta` from the yaw axis.

---

## 5. Simplified vs. Complex: `if (IsSet(Simplified))` → Strategy/Policy

The `IsSet(Simplified)` check appears in at least 6 locations (aero, roll, pitch, yaw, eom, ground effect):

**Before:**
```cpp
// aero.cpp
if (IsSet(Simplified)) {
    if (fabs(beta)*0.6F > tempAlpha)
        dragAlpha = 0.75F * (float)fabs(beta) * 0.6F;
    else
        dragAlpha = 0.75F * tempAlpha;
} else {
    if (fabs(beta)*0.6F > tempAlpha)
        dragAlpha = (float)fabs(beta)*0.6F;
    else
        dragAlpha = tempAlpha;
}

// aero.cpp — ground effect
if (!IsSet(IsDigital) && pz > -groundZ - 200.0F) { ... }

// roll.cpp
if (stallMode != Spinning) {
    if (IsSet(Simplified))
        pscmd *= 0.8F;
    else {
        // F-16 specific alpha error...
    }
}
```

**After** — policy-based or strategy pattern:
```cpp
// fcs_policy.hpp
struct IFcsPolicy {
    virtual ~IFcsPolicy() = default;
    virtual float dragAlphaScale() const = 0;
    virtual float rollAuthorityScale() const = 0;
    virtual bool enableGroundEffect() const = 0;
    virtual bool useAlphaErrorCoupling() const = 0;
};

struct PlayerFcsPolicy : IFcsPolicy {
    float dragAlphaScale() const override { return 1.0f; }
    float rollAuthorityScale() const override { return 1.0f; }
    bool enableGroundEffect() const override { return true; }
    bool useAlphaErrorCoupling() const override { return true; }
};

struct AiFcsPolicy : IFcsPolicy {
    float dragAlphaScale() const override { return 0.75f; }  // less speed bleed
    float rollAuthorityScale() const override { return 0.8f; }
    bool enableGroundEffect() const override { return false; }
    bool useAlphaErrorCoupling() const override { return false; }
};

// aero.cpp — clean, no flags
dragAlpha = tempAlpha * policy_.dragAlphaScale();

// Or even simpler with std::function / lambdas if you don't want full polymorphism:
struct FcsConfig {
    float dragAlphaScale = 1.0f;
    float rollAuthority = 1.0f;
    bool groundEffect = true;
    bool alphaErrorCoupling = true;
};

// Construct:
FcsConfig playerConfig{};           // defaults = full fidelity
FcsConfig aiConfig{.dragAlphaScale = 0.75f, .groundEffect = false};
```

---

## 6. The `Exec()` Pipeline → Type-Safe Step Chain

**Before** — hardcoded sequence, easy to get wrong, hard to instrument:
```cpp
void AirframeClass::Exec() {
    for (int i = 0; i < SimLibMinorPerMajor; i++) {
        Atmosphere();
        FlightControlSystem();
        Aerodynamics();
        if (nEngines == 2) MultiEngineModel(dt); else EngineModel(dt);
        RandomFailureModel();
        SetFlapsPlayer();
        Accelerometers();
        EquationsOfMotion(dt);
    }
    RunLandingGear();
}
```

**After:**
```cpp
// pipeline.hpp
using SimStep = std::function<void(float dt)>;

class SimulationPipeline {
public:
    void addStep(std::string name, SimStep step) {
        steps_.push_back({std::move(name), std::move(step)});
    }

    void run(float dt, int substeps) {
        for (int i = 0; i < substeps; ++i) {
            for (auto& [name, step] : steps_) {
                auto t0 = std::chrono::high_resolution_clock::now();
                step(dt);
                auto t1 = std::chrono::high_resolution_clock::now();
                // Optional: profile timing per step
                profile_[name] += std::chrono::duration<double>(t1-t0).count();
            }
        }
    }

private:
    std::vector<std::pair<std::string, SimStep>> steps_;
    std::unordered_map<std::string, double> profile_;
};

// Construction — order is explicit and data-driven
SimulationPipeline buildPipeline(Airframe& af) {
    SimulationPipeline pipeline;
    pipeline.addStep("atmosphere",  [&af](float dt) { af.atmos().update(dt); });
    pipeline.addStep("fcs",        [&af](float dt) { af.fcs().update(dt); });
    pipeline.addStep("aero",       [&af](float dt) { af.aero().update(dt); });
    pipeline.addStep("engine",     [&af](float dt) { af.engine().update(dt); });
    pipeline.addStep("failure",    [&af](float dt) { af.failures().update(dt); });
    pipeline.addStep("flaps",      [&af](float dt) { af.flaps().update(dt); });
    pipeline.addStep("eom",        [&af](float dt) { af.eom().update(dt); });
    return pipeline;
}
```

This also makes it trivial to add/remove steps for debugging, or run only a subset.

---

## 7. Integration Methods: `FLTust`/`FIAdamsBash`/`F7Tust` → `std::variant` of Integrators

The codebase has 3 integration methods used in different places, each with a magic name and a raw `float[]` history buffer:

**Before:**
```cpp
// pitch.cpp
alpha = Math.F7Tust(aoacmd, tp01*pitchMomentum, tp02*pitchMomentum,
                     tp03*pitchMomentum, dt, oldp03, &jp01);

// roll.cpp
pstab = Math.FLTust(pscmd, tr01*rollMomentum, dt, oldr01);

// pitch.cpp (integral term)
eintg = Math.FIAdamsBash(eintg1, dt, oldp02);

// All use raw SAVE_ARRAY (float[6]) history buffers
```

**After:**
```cpp
// integrators.hpp
class FirstOrderLag {
    // Replaces FLTust
public:
    float update(float command, float timeConstant, float dt) {
        // τ·dy/dt + y = cmd  →  y(n+1) = y(n) + (cmd - y(n))·dt/τ
        float alpha = dt / (timeConstant + dt);
        state_ = state_ + alpha * (command - state_);
        return state_;
    }
private:
    float state_{0.0f};
};

class BilinearIntegrator {
    // Replaces F7Tust (7-term Tustin)
    // The 7-term version is a Tustin transform with pre-warping
    // and multiple poles — can be modeled as a cascaded biquad
public:
    BilinearIntegrator(float b0, float b1, float a1, float a2)
        : b0_(b0), b1_(b1), a1_(a1), a2_(a2) {}

    float update(float input, float dt) {
        float output = b0_ * input + b1_ * x1_ - a1_ * y1_ - a2_ * y2_;
        x1_ = input;
        y2_ = y1_;
        y1_ = output;
        return output;
    }
private:
    float b0_, b1_, a1_, a2_;
    float x1_{0}, y1_{0}, y2_{0};
};

class AdamsBashforth4 {
    // Replaces FIAdamsBash (4-point Adams-Bashforth)
public:
    float update(float input, float dt) {
        float output = history_[0]
            + dt * (55.0f/24.0f * input
                   - 59.0f/24.0f * history_[1]
                   + 37.0f/24.0f * history_[2]
                   - 9.0f/24.0f  * history_[3]);
        history_[3] = history_[2];
        history_[2] = history_[1];
        history_[1] = history_[0];
        history_[0] = output;
        return output;
    }
private:
    std::array<float, 4> history_{};
};

// Usage in pitch axis:
class PitchAxis {
    BilinearIntegrator alphaFilter_;
    AdamsBashforth4    integralFilter_;
    FirstOrderLag      rateLimiter_;

public:
    void update(float aoacmd, float dt) {
        alpha_ = alphaFilter_.update(aoacmd, dt);
        integral_ = integralFilter_.update(integralCmd_, dt);
    }
};
```

No more raw `SAVE_ARRAY oldp01[6]` buffers with magic indices. Each integrator owns its state.

---

## 8. Raw Memory Management → Smart Pointers + RAII

**Before:**
```cpp
// readin.cpp
aeroDataset = new AeroDataSet[num_sets];
// ... scattered deletes in FreeAllAirframeData
delete[] aeroDataset->aeroData->mach;
delete[] aeroDataset->aeroData->alpha;
delete[] aeroDataset->aeroData->clift;
// ...

// airframe.h
AeroData *aeroData;           // raw pointer, who owns it?
AuxAeroData *auxaeroData;     // raw pointer, who owns it?
EngineData *engineData;       // raw pointer, who owns it?
RollData *rollCmd;            // raw pointer, who owns it?
```

**After:**
```cpp
// aero_data.hpp — value types with std::vector internals
struct AeroData {
    Table2D<float> cl, cd, cy;
    float clFactor{1.0f}, cdFactor{1.0f}, cyFactor{1.0f};
    // No destructor needed — Table2D uses std::vector
};

// dataset.hpp
struct AeroDataSet {
    AeroData          aero;
    EngineData        engine;
    RollData          fcs;
    AuxAeroData       aux;
    std::vector<Limiter> limiters;
};

// Global store — owns all data
class AircraftDatabase {
public:
    static AircraftDatabase& instance() {
        static AircraftDatabase db;
        return db;
    }

    const AeroDataSet& get(int vehicleIndex) const {
        return datasets_.at(vehicleIndex);
    }

    void loadAll() {
        auto types = parseAircraftList("sim/acdata/actypes.lst");
        datasets_.reserve(types.size());
        for (auto& type : types) {
            datasets_.push_back(loadAircraftData(type));
        }
    }
private:
    std::vector<AeroDataSet> datasets_;
};
```

Zero `new`/`delete`. Zero memory leaks. Copy/move semantics work automatically.

---

## 9. `AuxAeroData` 500-Field Struct → Named Groups

The `AuxAeroData` struct in `airframe.h` has roughly 500 fields, many of them loosely related. This is a maintenance nightmare.

**Before:**
```cpp
class AuxAeroData {
public:
    float fuelFlowFactorNormal;
    float fuelFlowFactorAb;
    float minFuelFlow;
    float normSpoolRate;
    float abSpoolRate;
    float jfsSpoolUpRate;
    // ... 490 more fields in a flat list
    int sndInt;
    int sndAbInt;
    int sndExt;
    int sndAbExt;
    int sndWind;
    int sndEject;
    LookupTable sndIntChart, sndAbIntChart; // ...
};
```

**After** — group into sub-structs:
```cpp
struct EngineParams {
    float fuelFlowFactorNormal{1.0f};
    float fuelFlowFactorAb{1.0f};
    float minFuelFlow{0.0f};
    float normSpoolRate{0.5f};
    float abSpoolRate{0.3f};
    float jfsSpoolUpRate{0.0f};
    float jfsSpoolUpLimit{0.25f};
    float lightupSpoolRate{0.0f};
    float flameoutSpoolRate{0.5f};
    float jfsRechargeTime{0.0f};
    float jfsMinRechargeRpm{0.0f};
};

struct FlapParams {
    int hasLef{0}, hasTef{0}, hasFlapperons{0};
    float tefMaxAngle{20.0f};
    float lefMaxAngle{25.0f};
    int tefNStages{1}, lefNStages{1};
    float tefRate{10.0f}, lefRate{10.0f};
    float CLtefFactor{0.0f}, CDtefFactor{0.0f};
    float CDlefFactor{0.0f};
    float lefMaxMach{1.4f};
};

struct FuelSystemParams {
    float fuelFwdRes, fuelAftRes;
    float fuelFwd1, fuelAft1;
    float fuelWingAl, fuelWingFr;
    float fuelFwdResRate, fuelAftResRate;
    // ... transfer rates
    float fuelMinFwd, fuelMinAft;
};

struct SoundParams {
    int sndInt{0}, sndAbInt{0}, sndExt{0}, sndAbExt{0};
    LookupTable sndIntChart, sndAbIntChart;
    // ...
};

struct AuxAeroData {
    EngineParams    engine;
    FlapParams      flaps;
    FuelSystemParams fuel;
    GearParams      gear;
    SoundParams     sound;
    TfrParams       tfr;       // terrain following radar
    RefuelParams    refuel;
    AnimationParams animation;
    // ... maybe 15 named groups instead of 500 flat fields
};
```

The parser in `readin.cpp` already uses a descriptor table — it just needs to map field names to nested struct offsets instead of a flat array.

---

## 10. Limiter System → `std::function` or `std::map<string, Table1D>`

**Before** — enum-indexed lookup:
```cpp
enum LimiterType { PitchYawControlDamper, RollControlDamper, RollRateLimiter,
                    PosGLimiter, NegGLimiter, AOALimiter, ... };
// Usage:
Limiter *limiter = gLimiterMgr->GetLimiter(RollRateLimiter, vehicleIndex);
if (limiter) pscmd *= limiter->Limit(alpha);
```

**After** — string-keyed or type-safe:
```cpp
class LimiterSet {
public:
    void set(std::string name, Table1D table) {
        limiters_[std::move(name)] = std::move(table);
    }

    std::optional<float> limit(std::string_view name, float x) const {
        auto it = limiters_.find(name);
        if (it == limiters_.end()) return std::nullopt;
        return it->second.interpolate(x);
    }

    float limitOr(std::string_view name, float x, float defaultVal = 1.0f) const {
        return limit(name, x).value_or(defaultVal);
    }

private:
    std::unordered_map<std::string, Table1D> limiters_;
};

// Usage — self-documenting, no enum coupling:
pscmd *= limiters.limitOr("roll_rate", alpha, 1.0f);
nycmd  *= limiters.limitOr("alpha_limiter", alpha, 1.0f);
plsdamp = limiters.limitOr("pitch_yaw_damper", qbar, 1.0f);
```

---

## Summary: Refactoring Priority

| Priority | Refactoring | Impact | Effort |
|----------|------------|--------|--------|
| **1** | `Table2D<T>` for aero/engine tables | Eliminates ~30 call sites with raw arrays, adds bounds safety | Medium |
| **2** | Composition of `AirframeClass` | Breaks 200+ member variable god-class into testable parts | High |
| **3** | `std::variant` stall state machine | Eliminates 5+ duplicated switch statements | Medium |
| **4** | `IEngineModel` strategy | Eliminates 1172-line duplication, enables engine testing | Medium |
| **5** | Smart pointers / RAII for data | Eliminates all manual memory management | Low |
| **6** | Named sub-structs for `AuxAeroData` | Turns 500-field flat struct into navigable groups | Low |
| **7** | FCS policy for Simplified/Complex | Removes scattered `IsSet(Simplified)` checks | Low |
| **8** | Typed integrator classes | Eliminates magic `SAVE_ARRAY` buffers and cryptic `F7Tust` calls | Medium |
| **9** | Pipeline for `Exec()` | Enables profiling, debugging, step reordering | Low |
| **10** | String-keyed limiters | Self-documenting, removes enum coupling | Low |

Items 1, 5, 6, and 10 are the low-hanging fruit — high readability gains with relatively small, mechanical changes. Items 2, 3, and 4 are the architectural wins that make the codebase genuinely extensible for future aircraft types or FCS configurations.