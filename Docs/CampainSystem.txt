I'll research the Falcon 4 dynamic campaign mission generation logic from the FreeFalcon source code. Let me explore the repository and analyze the relevant code.
Now let me dive deep into the actual source code of the critical mission generation files.
Now let me get the remaining pieces - package assembly, squadron management, and the GTM/NTM for completeness.
......
Here's a comprehensive breakdown of the Falcon 4 dynamic campaign mission generation logic, drawn directly from the FreeFalcon source code.

---

# Falcon 4 Dynamic Campaign — Mission Generation Logic

## 1. Architectural Overview

The system uses a **request → queue → fulfill** pipeline built on a VU (Virtual Universe) distributed entity framework. Campaign entities (Squadrons, Flights, Packages, Objectives) are all VU entities with network IDs, ownership, and serialization. The pipeline has three distinct layers:

1. **Request Generators** — Various campaign subsystems (strategy, tactics, objectives, event triggers, player calls) create `MissionRequestClass` messages and send them to the team's Air Tasking Manager.
2. **Air Tasking Manager (ATM)** — The central arbiter. Receives requests, sorts them by priority, deconflicts, and attempts to fulfill each by building a `Package`.
3. **Package/Flight Builders** — `PackageClass::BuildPackage()` selects squadrons, creates flights, generates threat-avoiding routes, assigns loadouts, and assembles multi-flight packages with escorts, SEAD, and support.

The campaign runs on a **separate thread** (`CampaignThread`). A master `CampaignClass` object tracks time (`CurrentTime`) and controls when each domain's tasking and planning cycles fire by comparing timestamps like `lastAirTask`, `lastAirPlan`, etc. against the current campaign time.

**Entity hierarchy:**
```
FalconEntity (VU entity)
  → CampManagerClass (base for all tasking managers)
    → AirTaskingManagerClass
    → GroundTaskingManagerClass
    → NavalTaskingManagerClass
  → UnitClass → AirUnitClass
    → SquadronClass
    → PackageClass
    → FlightClass
```

---

## 2. The Mission Data Table — Central Configuration

Every mission type is defined by a static `MissionData[]` array in `mission.cpp`, indexed by `MissionTypeEnum`. Each entry is a `MissionDataType` struct containing ~20 fields that completely specify how that mission type behaves. This is the single most important configuration structure in the system.

**Key fields per entry:**

| Field | Purpose |
|---|---|
| `type` | Mission type ID |
| `target` | Target type: `AMIS_TAR_NONE/OBJECTIVE/UNIT/LOCATION` |
| `skill` | Primary ARO (Air Role) rating for squadron matching |
| `mission_profile` | Altitude profile: `MPROF_LOW`, `MPROF_HIGH`, or `MPROF_STANDARD` |
| `target_profile` | Waypoint pattern at target (`TPROF_ATTACK`, `TPROF_LOITER`, etc.) |
| `target_desc` | When route actions apply (TTL, ATA, or TAO) |
| `routewp` | Ingress/egress waypoint action type |
| `targetwp` | Target waypoint action (WP_STRIKE, WP_BOMB, WP_CAP, etc.) |
| `minalt` / `maxalt` | Min/max altitude at target (hundreds of feet) |
| `missionalt` | Suggested cruise altitude (hundreds of feet) |
| `separation` | Time offset for escort flights (seconds) |
| `loitertime` | Loiter/station time (minutes) |
| `str` | Default # of aircraft |
| `min_time` / `max_time` | Min/max planning advance time (minutes) |
| `escorttype` | Escort mission type (0 = none) |
| `mindistance` | Min distance between similar missions |
| `mintime` | Min time between similar missions |
| `caps` | Required vehicle capabilities (stealth, VTOL, etc.) |
| `flags` | Bitfield of ~20+ behavioral flags |

**Examples:**
- **BARCAP**: alt 10,000–40,000 ft, 15-min loiter, 2 aircraft, flags: `ADDAWACS | NOTHREAT | ADDTANKER | DONT_COORD | EXPECT_DIVERT | NO_BREAKPT | FLYALWAYS`
- **OCA Strike**: alt 500–12,000 ft, 4 aircraft, flags: `ADDBDA | AVOIDTHREAT | ADDSEAD | ADDESCORT | ADDBARCAP | ADDOCASTRIKE | MATCHSPEED | NO_TARGETABORT`
- **Deep Strike**: Same as OCA but adds `ADDAWACS | ADDECM | HIGHTHREAT`
- **Stealth Strike**: Requires `VEH_STEALTH` capability, no escorts, higher threat tolerance
- **Intercept**: `IMMEDIATE | ASSIGNED_TAR | EXPECT_DIVERT | FLYALWAYS`

---

## 3. Mission Types

There are **41 mission types** spanning counter-air, strike, ground attack, support, and special roles:

### Counter-Air (ARO_CA)
| Type | Description |
|---|---|
| `AMIS_BARCAP` / `AMIS_BARCAP2` | Barrier CAP — patrol a line/area to protect from enemy aircraft |
| `AMIS_HAVCAP` | High-value asset CAP — orbit over a specific asset |
| `AMIS_TARCAP` | Target CAP — patrol over a target area |
| `AMIS_RESCAP` | Rescue CAP — cover a SAR operation |
| `AMIS_AMBUSHCAP` | Ambush CAP — very low altitude (2,000–10,000 ft) surprise CAP |
| `AMIS_SWEEP` | Fighter sweep — clear enemy aircraft from an area |
| `AMIS_ALERT` | Ground alert — scramble on warning |
| `AMIS_INTERCEPT` | Intercept — immediate response to detected enemy aircraft |
| `AMIS_ESCORT` | Fighter escort — protect strike flights |

### Strike (ARO_S)
| Type | Description |
|---|---|
| `AMIS_OCASTRIKE` | Offensive Counter Air — destroy enemy airpower on the ground |
| `AMIS_INTSTRIKE` | Interdiction strike — cut supply/fuel lines |
| `AMIS_STRIKE` | General strike |
| `AMIS_DEEPSTRIKE` | Deep strike — far behind enemy lines, always gets escort+SEAD |
| `AMIS_STSTRIKE` | Stealth strike — flies alone on a safe path |
| `AMIS_SEADSTRIKE` | SEAD strike — destroy SAM/radar units |

### Ground Attack (ARO_GA)
| Type | Description |
|---|---|
| `AMIS_ONCALLCAS` | On-call CAS — orbits near front, waits for tasking |
| `AMIS_PRPLANCAS` | Pre-planned CAS — strikes at a scheduled time |
| `AMIS_CAS` | Immediate CAS — direct support |
| `AMIS_SAD` | Search and Destroy |
| `AMIS_INT` | Interdiction (supply lines) |
| `AMIS_BAI` | Battlefield Area Interdiction |

### Support & Special
| Type | Description |
|---|---|
| `AMIS_STRATBOMB` | Strategic bombing (ARO_SB) |
| `AMIS_AWACS` / `AMIS_JSTAR` / `AMIS_TANKER` | Orbit support (300-min loiter each) |
| `AMIS_ECM` | Standoff jamming |
| `AMIS_RECON` / `AMIS_BDA` | Reconnaissance / Battle Damage Assessment |
| `AMIS_FAC` | Forward Air Controller |
| `AMIS_SAR` | Search and Rescue |
| `AMIS_AIRLIFT` | Airlift / transport |
| `AMIS_ASW` | Anti-submarine warfare |
| `AMIS_ASHIP` | Anti-shipping |

---

## 4. Mission Request Creation

### 4.1 Entry Point — `RequestMission()`

The standard path for mission requests. Creates a `FalconMissionRequestMessage`, calls `GetPriority()` to compute a final priority value, and sends it to the team's ATM. **If priority ≤ 0, the request is silently dropped.**

### 4.2 Key Request Generators

**`ATM::DoCalculations()`** (auto-generation each cycle):
- For each active friendly airbase: generates an `AMIS_OCASTRIKE` against it with priority = `usage × 30`, TOT 30–90 minutes in the future
- For each carrier: generates `AMIS_ASHIP` missions
- Cycles through supply bases generating `AMIS_AIRLIFT` missions (one per base per cycle)
- Updates AWACS/JSTAR/Tanker/ECM location lists

**`RequestIntercept()`** — Creates an `AMIS_INTERCEPT` with `AMIS_IMMEDIATE` flag when AWACS detects incoming strike-type enemy aircraft. Stores the enemy's heading and the entire enemy package's air combat score as `match_strength`.

**`RequestSARMission()`** — Creates `AMIS_SAR` at a downed aircraft's location. Refuses if `DistanceToFront > MAX_SAR_DIST`.

**`TargetAllSites()`** — Batch-generates requests for all objectives within a primary objective's area:
- Airbases/Strips/Army Bases → `AMIS_OCASTRIKE` at TOT+15 min
- C3 Facilities → `AMIS_STRIKE` at TOT+15 min
- Radar sites → `AMIS_OCASTRIKE` targeting the radar feature specifically
- Factories/Depots/Refineries/Ports/Powerplants → `AMIS_INTSTRIKE` at TOT+25 min
- Bridges → `AMIS_INTSTRIKE` at TOT+25 min
- Air defense units → `AMIS_SEADSTRIKE` at TOT+5 min
- Also generates 3 sweep missions at TOT+5, +15, +25 minutes

### 4.3 Mission Context System

Each request carries a `mission_context` code (~60+ options) describing *why* the mission was requested: `enemyUnitAdvanceBridge`, `attackEnemyUnit`, `enemyStrikesExpected`, `interceptEnemyAircraft`, `friendlyRescueExpected`, `enemySupplyInterdictionBridge`, `enemyProductionSource`, `enemyAirDefense`, `AirActionPrepAD`, `AirActionPrepAB`, `AirActionOCA`, `AirActionInterdiction`, `AirActionCAS`, etc. This context feeds into the priority calculation.

---

## 5. Request Processing and Prioritization

### 5.1 `ProcessRequest()` — Queue Insertion

When a request arrives, the ATM:

1. **Adjusts target location** for support missions — snaps to nearest AWACS/ECM/Tanker orbit track
2. **Adjusts priority**:
   - `REQF_PART_OF_ACTION`: priority += 100 (capped at 255)
   - Not part of action: priority -= 25 (floored at 0)
3. **Handles unspotted targets**:
   - Objective targets: 20% chance → converted to `AMIS_RECON` (priority ÷ 4); 20% chance → dropped entirely
   - Unit targets: 33% chance → converted to `AMIS_SAD`
   - Naval targets: 66% chance → converted to `AMIS_PATROL` (priority ÷ 4)
4. **Deconflicts** against both active packages and queued requests — if same mission type + same target + within `mintime` and `mindistance` → drops the duplicate
5. Inserts into the **priority-sorted** `requestList` (sorted linked list, highest priority = last element)

### 5.2 Mission Filling Budget

`missionsToFill` limits how many non-essential missions are planned per cycle. It's incremented probabilistically when `RequestMission()` is called based on `actionTempo`. The `missionsFilled` counter resets each cycle.

---

## 6. The Main Tasking Cycle — `ATM::Task()`

Called once per campaign update cycle. This is the heart of mission generation.

### 6.1 Execution Flow

```
1. Guards: check team is active, local, has squadrons
2. Check flags: if neither ATM_NEW_REQUESTS nor ATM_NEW_PLANES set → return
3. Clear ATM_NEW_PLANES flag
4. For non-final packages: call Package::CheckNeedRequests() (generates sub-requests)
5. Iterate requestList from HIGHEST to LOWEST priority:
   a. Skip if missionsFilled >= missionsToFill (unless action mission or FLYALWAYS)
   b. If AMIS_IMMEDIATE → BuildDivert()
   c. Otherwise → BuildPackage()
   d. Clamp aircraft count to MissionData[].str
   e. Handle result:
      - PRET_SUCCESS → insert into packageList
      - PRET_DELAYED → move to delayedList (retry next cycle)
      - PRET_TIMEOUT → delete request permanently
      - Other → delete request
6. TIME BUDGET: return after 200ms of processing
7. Clear ATM_NEW_REQUESTS, move delayedList back to requestList
```

### 6.2 Time Budget

The ATM has a **200ms processing budget** per `Task()` call (`GetTickCount() - task_time > 200`). This prevents the campaign thread from stalling. If the budget expires mid-iteration, remaining requests wait for the next cycle.

---

## 7. Package Building — `BuildPackage()` → `PackageClass::BuildPackage()`

### 7.1 Target Analysis

1. Sets destination from the request
2. Initializes `package_flags` from `MissionData[].flags`
3. **For objective targets with multiple features**: iteratively selects up to 5 targets / 4 flights using `BestTargetFeature()`, preferring different feature types. Stops when objective status reaches 0%.
4. **Priority gating**: If priority > 150 (`ATM_HIGH_PRIORITY`), forces at least 2 strike flights.

### 7.2 Threat Assessment

1. Calls `ScoreThreatFast()` at min and max altitude to get low-alt and high-alt threat scores
2. If either exceeds `MIN_SEADESCORT_THREAT` → sets `NEED_SEAD` bit
3. If priority > 150 and both altitudes have nonzero threat → forces `NEED_SEAD`
4. **Cancellation**: If threat at BOTH altitudes exceeds `MAX_FLYMISSION_HIGHTHREAT` → returns `PRET_CANCELED`. If it exceeds `MAX_FLYMISSION_THREAT` → triggers `CallInOCAStrikes()` (enemy OCA response) and cancels.

### 7.3 Main Strike Flight Creation

For each target:
1. Calls `AttachFlight()` which either steals an in-flight aircraft (`AMIS_IMMEDIATE`) or calls `FindBestAir()` to select a squadron and creates a new `Flight` entity
2. Calls `flight->BuildMission(mis)` to generate the route, timing, and loadout
3. On the **first flight only**: extracts ingress/egress waypoints (assembly point, breakpoint, target area) and stores them in the Package for other flights to reference
4. **Bomber conversion**: If the squadron's strategic bombing rating exceeds its strike rating (and 75% random), converts mission to `AMIS_STRATBOMB`

### 7.4 Support Flight Addition

**SEAD Escort** (if `AMIS_ADDSEAD` AND `NEED_SEAD` detected):
- Creates `AMIS_SEADESCORT` flight targeting the main strike flight
- Aircraft count: `min(MissionData[SEADESCORT].str, main_flight_aircraft)`
- TOT offset: `MissionData[SEADESCORT].separation × CampaignSeconds`

**Fighter Escort** (if `AMIS_ADDESCORT` — always added):
- 50% chance of SEAD escort vs fighter escort (unless `escorttype` specified)
- Targets the main strike flight (`element[0]`)

**FAC** (if `AMIS_ADDFAC` and exactly 1 flight):
- Creates `AMIS_FAC` flight, TOT 5 minutes early, using reserves
- If no FAC available: converts ONCALLCAS to `AMIS_PRPLANCAS` or cancels

**Enemy Reaction Triggers** (the package proactively generates enemy missions):
- `AMIS_ADDBARCAP` → sends enemy `AMIS_BARCAP` request with `match_strength = aa_strength`, priority = `mis->priority / 5`
- `AMIS_ADDSWEEP` → sends enemy sweep request at random TOT offset ±120 sec

### 7.5 Support Flight Search — `FindSupportFlights()`

Searches existing active packages for compatible support:
- **AWACS/JSTAR/ECM/Tanker**: Finds closest existing flight that has taken off and will remain on station past `mis->tot + (loitertime + 10) × CampaignMinutes`
- **Tanker**: Prefers proximity to the ingress assembly point over target
- If no existing support found, creates **new mission requests**:
  - Tanker: priority 50, `REQF_NEEDRESPONSE | REQF_ONETRY`, sets `wait_for |= AMIS_ADDTANKER`
  - AWACS: context `friendlyAWACSNeeded`
  - ECM: only if `NEED_ECM` bit set, context `enemyRadarPresent`

### 7.6 Package Finalization — `CheckNeedRequests()`

Called each cycle. Manages a **wait/timer system**:
1. Decrements `wait_cycles` each call
2. When timer expires OR past takeoff deadline:
   - Gives up on pending BARCAP/SWEEP requests
   - If `AMIS_ADDBDA` and priority > `MINIMUM_BDA_PRIORITY`: adds BDA flight
   - If tanker needed and `g_bTankerWaypoints`: inserts tanker waypoints into each flight
   - **Finalizes all child flights** via `FinalizeFlight()` — removes unused waypoints, sets assembly coordination
   - Sets `SetFinal(1)` — package is now locked and active

---

## 8. Squadron Selection — `FindBestAir()`

This is the core squadron-picker, scoring each squadron in the team's list:

### 8.1 Hard Filters (must pass ALL)

1. **Not relocating**: `squadronRetaskAt > currentTime` → skip
2. **Skill threshold**: Base score = `(rating[role] + 4) / 5` must exceed `(255 - priority) / 25`
3. **Specialty match**: +5 if squadron specialty matches role (AA or AG), -5 if mismatched
4. **Capability check**: `(caps & stats) == caps` — must have all required capabilities (stealth, night, VTOL, etc.)
5. **Stealth restriction**: Stealth aircraft only fly at night (`TimeOfDayGeneral(tot) != TOD_NIGHT`)
6. **NPC-only filter**: `AMIS_NPC_ONLY` missions skip squadrons with valid player aircraft types
7. **Range check**: `distance + loiter_distance ≤ squadron_range`. Short-range aircraft get loiter reduced to min(45, original)
8. **Speed matching**: If `AMIS_MATCHSPEED`, aircraft max speed must be ≥ required and cruise ≤ 1.2× required
9. **Schedule check**: The airbase's 5-minute time block must not be fully occupied (`ATM_CYCLE_FULL = 0x1F`)
10. **Aircraft availability**: `FindAvailableAircraft()` must return ≥ `aircraft - 1` (or ≥ 1 if using reserves)

### 8.2 Scoring Adjustments

| Condition | Bonus |
|---|---|
| Player squadron (priority tier dependent) | +1 to +3 |
| Already assigned to this package | +3 |
| Same airbase as previous assignment | +2 |
| Within half range | +2 |
| Same-owner carrier (transport) | +5 |
| Insufficient aircraft | -5 |
| Quickest arrival time | +2 |

### 8.3 CA Mission Sizing

If `match_strength > 0` and role is `ARO_CA`: defaults to 4 aircraft; reduces to 2 if the aircraft's `HitChance[Air] × 1.5 > match_strength` (avoiding overkill).

---

## 9. Aircraft Availability and Scheduling

### 9.1 `FindAvailableAircraft()`

1. Computes available pool: `max(percentage × totalVehicles / 100, minimum_available)` — configurable via `g_npercentage_available_aircraft` and `g_nminimum_available_aircraft`
2. If `REQF_USERESERVES`: uses ALL vehicles instead
3. For each time block from `start_block` to `final_block`: clears availability for blocks where the schedule bit is set
4. Returns the number of available aircraft and fills `mis->slots[]`

### 9.2 Airbase Schedule System

Each airbase has a `uchar schedule[32]` array — 32 bytes, each representing a **5-minute time block** (160 minutes total). Each bit in a byte represents one minute's runway occupancy (5 bits used per byte, `ATM_CYCLE_FULL = 0x1F` means all 5 minutes occupied).

**`FindTakeoffSlot()`**: Tries the requested slot, then +1, +2 minutes forward, then up to 10 slots backward. Returns 0xFFFFFFFF on failure.

**`ScheduleAircraft()`**: Marks takeoff slot and next block. For flights > 2 aircraft: marks an additional slot. For single-runway bases: also schedules landing slots.

**`ZapAirbase()`** (damage response):
- 0 runways: zaps entire schedule for repair duration
- 1 runway: zaps every other block
- 2+ runways: no change
- Cancels all flights planned during zapped blocks

### 9.3 Pilot Assignment — `AssignPilots()`

- **Slot 0 (Flight Lead)**: Scans first third of pilot roster for `PILOT_AVAILABLE` (commanders)
- **Remaining slots**: Scans backward from end of roster (wingmen)
- If no pilot found for any slot → flight fails

### 9.4 Rating Decay

After assignment: `new_rating = (int)(0.75 × current_rating) + 1` — reduces the squadron's effectiveness rating for that role by ~25% to prevent the same squadron from being picked repeatedly in a single cycle.

---

## 10. Route Planning and Waypoint Generation

### 10.1 `BuildPathToTarget()` — Master Route Builder

For each flight in a package:

1. Create **takeoff WP** at airbase location
2. **Ingress** (unless `AMIS_TARGET_ONLY`):
   - `SetupIngressPoints()` for the main flight, `AddIngressPath()` for escorts
   - Creates assembly point and breakpoint
3. **Target area waypoints** based on `target_profile`:
   - `TPROF_ATTACK` → IP → Target → Turn point (+ Assembly, Breakpoint)
   - `TPROF_LOITER`/`TPROF_SEARCH` → Target + looping WPs (racetrack pattern)
   - `TPROF_SWEEP` → 3 circular turn points forming sweep arc
   - `TPROF_FLYBY` → single target overflight
   - `TPROF_LAND` → land + station + takeoff
   - `TPROF_HPATTACK`/`TPROF_TARGET` → target only
   - `TPROF_AVOID` → breakpoint + turn point
4. **Egress**: `SetupEgressPoints()` or `AddEgressPath()`
5. **Landing WP** at home base
6. **High-altitude WP** 30 NM before home for long-range missions (>40 NM from base)
7. **Cleanup**: `EliminateExcessWaypoints()` trims unnecessary filler waypoints

### 10.2 Threat-Avoiding Ingress — `SetupIngressPoints()`

1. Creates a temporary WP at the target location
2. Calls `CheckSafePath()` — if threat exceeds `MIN_AVOID_THREAT`:
   - Calls `FindSafePath()` which uses **A* pathfinding** with `PATH_ENEMYCOST` flag
   - The A* extend function `GetNeighborCoord()` samples `ScoreThreatFast()` at the cell center and 4 cardinal neighbors (at `MAP_RATIO` intervals), taking the **maximum**. If > 120 → cell is impassable
   - Otherwise: `cost = base_cost × step + threat_cost / 2.0`
   - Heuristic multiplier: Air = 4.0 (encourages finding routes around threats)
3. Calls `AddSafeWaypoint()` to find an assembly point at `MIN_AP_DISTANCE` from target, in friendly territory, outside threat zones, and away from the FLOT
4. Adds a breakpoint at `BREAKPOINT_DISTANCE × 2` before target
5. **Saves** assembly/breakpoint locations in the Package for all other flights to share

### 10.3 Altitude Selection — `CheckBestAltitude()`

Three altitude levels are computed for each flight:

| Altitude | Purpose | Selection Logic |
|---|---|---|
| `sTargetAlt` | At the target | From `MissionData[].missionalt`, clamped to min/max. `CheckBestAltitude()` samples threat scores at 5 nearby grid points across all altitude levels, with a -20 bonus for the preferred altitude |
| `sMissionAlt` | Ingress/egress | Low profile = `LowAlt × 100`; High = `HighAlt × 100`. For `MPROF_STANDARD`: uses high profile unless enemy CA missions exceed `MAX_ENEMY_CA_MISSIONS_FOR_HIGH_PROFILE` |
| `sCruiseAlt` | Over friendly territory | `CruiseAlt × 100` unless very close to front/target |

All altitudes are randomized with `LevelIncrement` and snapped to **500-foot increments**. Climb rate is limited by `g_fClimbRatio` (max altitude change per distance unit).

### 10.4 Threat Scoring on Routes — `ScoreThreatsOnWPLeg()`

Samples `ScoreThreatFast()` at `MAP_RATIO` intervals along each waypoint leg. Returns worst-point (`TT_MAX`), sum (`TT_TOTAL`), or average (`TT_AVERAGE`).

`CheckPathThreats()` walks the entire route calling `CollectThreatsFast()` every 5 km and returns `NEED_SEAD` and/or `NEED_ECM` flags — this drives whether escort flights are added.

### 10.5 `ScoreThreatFast()` — The Threat Model

Uses **pre-computed** `SamMapData` and `RadarMapData` arrays (not real-time unit queries — this is critical for performance):

| Altitude Band | Formula |
|---|---|
| Low | `SAM_low × 28 + SAM_high × 2 + 10` (if enemy territory) |
| Medium | `SAM_low × 10 + SAM_high × 23` |
| High | `SAM_high × 30` |
| Very High | `SAM_high × 15` |

The team bit offset determines which team's SAM data is read. If the territory owner doesn't allow overflight → returns 32,000 (lethal).

### 10.6 Waypoint Elimination — `EliminateExcessWaypoints()`

Two-pass cleanup:
1. **Collinearity**: If a WP's angle deviation > 10° or < 120° (sharp turn) → keep it; otherwise eliminate
2. **Threat reduction**: If the direct path's threat cost ≤ sum of the two sub-legs' costs → eliminate the middle WP (it wasn't helping)

### 10.7 Timing Calculation — `SetWPTimes()`

Two-pass algorithm:
1. **Forward pass** (target → landing): calculates speeds and arrival times using `TimeToArrive(distance, speed)`. Speed range: `0.7 × CruiseSpeed` to `1.3 × MaxSpeed`
2. **Backward pass** (target → takeoff): calculates departure times to meet the TOT
3. The TOT waypoint is identified by `WPF_TARGET` flag, then `WPF_TURNPOINT`, then first WP
4. Takeoff, Target, and Assembly points get `WPF_TIME_LOCKED`
5. Escort separation applied as `MissionData[].separation × CampaignSeconds` offset

### 10.8 Tanker Waypoint Insertion

If `fuelNeeded > 2/3 × loaded_fuel` → adds an ingress tanker WP. Always tries to add an egress tanker WP. Uses `FindNearestActiveTanker()` to locate active tanker tracks. Checks fuel reachability with 50% tolerance (`fuelNeeded > fuel × 1.5` = unreachable → flight is killed).

---

## 11. A* Pathfinding Engine

### 11.1 Architecture

A singleton `ASD` (`AS_DataClass`) with:
- **Priority queue**: sorted linked list by f = g + h (cost + heuristic)
- **Closed set** (`tried`): already-expanded nodes
- **Free list** (`waste`): pool of 2000 pre-allocated nodes (zero dynamic allocation)
- **Thread safety**: critical sections via `AS_THREAD_SAFE`

### 11.2 Algorithm

Standard A* with key modifications:
- **Max 2000 nodes** expanded per search (`MAX_SEARCH`)
- **Max 96 steps** path length
- **8-directional** grid movement
- **Partial path on failure**: if `RETURN_PARTIAL_ON_FAIL` flag set, returns the node with lowest heuristic from the explored set
- **Node pooling**: Pre-allocated array of 2000 nodes linked as free list; no `malloc`/`new` during search

### 11.3 Air Path Cost Function

For each grid cell neighbor:
1. Base cost from `CostTable[terrain][movement]` (air = always 1.0)
2. **Threat sampling**: `ScoreThreatFast()` at cell center + 4 cardinal offsets → take **maximum**
3. If max threat > 120 → **impassable** (no-fly zone)
4. Otherwise: `cost = 1.0 × step + threat / 2.0`
5. Heuristic: `distance × 4.0` (strongly encourages direct paths, but threat costs create detours)

---

## 12. Loadout Selection

### 12.1 Mission-Specific Weapon Loading

| Mission | Loadout Strategy |
|---|---|
| SEAD Strike (with radar target) | 6× anti-radiation missiles (HARMs) |
| SEAD Strike (escort) | DEAD loadout + 2× anti-radiation |
| OCA Strike | 6× generic weapons |
| Interdiction/Strike/Deep Strike | 4× generic weapons |
| Strategic Bomb | Dumb bombs only (`WEAP_DUMB_ONLY`) |
| FAC | FAC loadout + BAI loadout |
| CAS/SAD/BAI/INT | BAI loadout |
| ASW | DEAD loadout |
| Recon/BDA/Patrol | Recon loadout (1 weapon) |
| CAP/Sweep/Intercept/Escort | No special loadout (AA defaults) |
| ECM | ECM jammer loadout |

### 12.2 Common Additions (all missions)

1. **AA missiles**: 50% chance → 2× radar-guided + 4× heat-seekers
2. **Remaining hardpoints**: filled with generic AA weapons
3. **Jammer**: checks built-in jammer (`VEH_HAS_JAMMER`) or loads external ECM pod
4. **Chaff/Flare pods**: always attempted
5. **Laser pod**: added if any laser-guided bombs loaded
6. **GPS-guided bombs**: for CAS/SAD/INT/BAI, also adds laser pod

### 12.3 `WeaponLoadScore()` — Weapon Selection

Complex scoring with many modifiers:
- Base score from `GetWeaponScore()` weapon data tables
- Nuclear weapons always score 0 (AI restriction)
- Type flag matching: +100 bonus
- BAI restrictions: rejects laser-guided, radar-guided, anti-radiation missiles
- DEAD scoring: `score × Range / 100`; precision weapons get bonus `× 200 / (Strength + 1)`
- Randomization: 50% chance of dividing score by 4 (variety). Same weapon as last: A/G gets 4× (consistency), A/A gets ÷4 (diversity)
- TV-guided: score 0 at night, ÷4 at dawn/dusk
- Ammo scarcity: score scaled proportionally if squadron has < 100 of the weapon

### 12.4 Hardpoint Loading Strategy

Iterates from centerline outward. For even loads: loads symmetrically on paired hardpoints. HP 0 is always the gun (non-negotiable, free). ECM/Laser pods can overwrite existing AA/AG weapons.

---

## 13. Ground Tasking Manager (GTM)

While the ATM handles air missions, the GTM handles ground unit assignment in a parallel but structurally similar system:

### 13.1 Objective Scoring

Each campaign cycle, `DoCalculations()` scores all objectives:
- **Front proximity**: `(200 - DistanceToFront) × 0.2`, capped at ±30
- **Enemy strength**: +1 per 50 enemy vehicles within 100 km of front, capped at 30
- **Priority bonus**: +50 if priority > 95, +20 if > 90
- Combined: `front_score + enemy_score + random(5)`, clamped [0, 100]

### 13.2 Unit Collection and Assignment

`Task()` operates in phases:
1. **Collection**: Iterates all friendly ground units, filters by team, calls `RallyUnit()` and `UpdateParentStatistics()`, assigns to order-type lists based on role scores
2. **Assignment priority** varies by action type (Offensive/Defensive/Consolidate):
   - **Offensive**: Capture(fastest) → Assault(best) → Airborne(best) → Commando(best) → Secure(fastest) → Defend(best) → AirDefense(fastest) → Support → Repair → Radar → Reserve(best)
   - **Defensive**: Defend(fastest) only
3. **Scoring**: Each unit-objective pair is scored considering distance, role match, unit strength, and objective priority

### 13.3 Interaction with Air Missions

The GTM doesn't directly generate air missions, but ground objective priorities feed into the strategy layer that creates air mission requests. Ground units under attack generate `AMIS_CAS` requests. Ground objectives targeted by the GTM become targets for `AMIS_OCASTRIKE` and `AMIS_INTSTRIKE`.

---

## 14. Key Tuning Parameters Summary

| Parameter | Value | Effect |
|---|---|---|
| `ATM_MAX_CYCLES` | 32 | 32 × 5 min = 160 min planning window |
| `ATM_CYCLE_FULL` | 0x1F | 5 bits = 5 minutes per schedule block |
| Task time budget | 200 ms | Max processing time per `Task()` call |
| Mission delay increment | 30 min | TOT push when delayed |
| Max delays before timeout | 8 | After 8 delays → permanent cancel |
| `ATM_HIGH_PRIORITY` | 150 | Threshold for forced multi-strike, forced SEAD |
| Threat impassable threshold | 120 | A* cells with threat > 120 are blocked |
| Ingress tanker threshold | 2/3 fuel | Add tanker if fuel needed exceeds this |
| Fuel reachability tolerance | 50% | `fuelNeeded > fuel × 1.5` = unreachable |
| Altitude snap | 500 ft | All altitudes rounded to 500 ft increments |
| Speed range for timing | 0.7× to 1.3× cruise | Acceptable speed bounds for WPTimes |
| Refuel time at tanker | 3 min | Station time for tanker WP |
| Action priority boost | +100 | For missions part of an action plan |
| Non-action priority penalty | -25 | For standalone missions |
| Rating decay | 25% | Squadron rating reduction per assignment |
| A* max nodes | 2000 | Maximum search expansion |
| A* max path length | 96 steps | Longest possible path |
| Max scramble distance | 60 grid units | Range for alert/scramble intercept |

---

## 15. Complete Code Flow Diagram

```
[Strategy Layer / Objectives / Events / Player Requests]
        │
        │ Creates MissionRequestClass
        │ Calls RequestMission()
        ▼
[FalconMissionRequestMessage] ──→ Team's ATM
        │
        ▼
[ATM::ProcessRequest()]
  ├─ Adjust target location (AWACS/tanker/ECM track snap)
  ├─ Adjust priority (±25/100 based on action type)
  ├─ Convert unspotted targets → RECON/SAD/PATROL
  ├─ Deconflict vs active packages + queued requests
  └─ Insert into priority-sorted requestList
        │
        ▼
[ATM::Task()] ─── per campaign cycle (200ms budget)
  │
  ├─ For each request (highest priority first):
  │   │
  │   ├─ [AMIS_IMMEDIATE?] ──→ BuildDivert()
  │   │                            └─ FindBestAirFlight() → FalconDivertMessage
  │   │
  │   └─ [Scheduled?] ──→ BuildPackage()
  │       │
  │       ▼
  │   [PackageClass::BuildPackage()]
  │       ├─ Target analysis (feature selection, multi-target)
  │       ├─ Threat assessment (ScoreThreatFast at min/max alt)
  │       ├─ For each target → AttachFlight():
  │       │   ├─ FindBestAir() → squadron selection (scored)
  │       │   │   ├─ Filter: skill, specialty, caps, range, schedule, availability
  │       │   │   └─ Score: player bonus, base proximity, arrival time
  │       │   ├─ NewFlight() → create Flight entity
  │       │   └─ flight->BuildMission():
  │       │       ├─ BuildPathToTarget():
  │       │       │   ├─ SetupIngressPoints() ──→ FindSafePath() (A* + threat)
  │       │       │   ├─ Add target profile WPs (TPROF_ATTACK/LOITER/SWEEP/etc.)
  │       │       │   ├─ SetupEgressPoints()
  │       │       │   ├─ EliminateExcessWaypoints()
  │       │       │   └─ AddTankerWayPoint() (if fuel needed > 2/3)
  │       │       ├─ SetWPTimes() (forward + backward pass)
  │       │       ├─ SetupAltitudes() + CheckBestAltitude()
  │       │       ├─ FindTakeoffSlot() (airbase schedule)
  │       │       ├─ LoadWeapons() (mission-specific + common additions)
  │       │       └─ AssignPilots()
  │       ├─ Add SEAD escort (if NEED_SEAD detected)
  │       ├─ Add fighter escort (if AMIS_ADDESCORT)
  │       ├─ Add FAC (if AMIS_ADDFAC)
  │       ├─ Trigger enemy BARCAP/SWEEP requests
  │       └─ FindSupportFlights() (AWACS/JSTAR/ECM/Tanker)
  │
  ▼
[CheckNeedRequests()] ─── over multiple cycles
  ├─ Wait for support responses (tanker, AWACS, etc.)
  ├─ Add BDA flight (if AMIS_ADDBDA)
  ├─ FinalizeFlight() for all children
  └─ SetFinal(1) → package is locked and active
        │
        ▼
[Active Packages with Flights, Waypoints, and Loadouts]
  └─ Camp2Sim bridge converts to simulation entities for 3D flight
```

---

This architecture cleanly separates **request generation** (distributed across campaign systems), **request management and prioritization** (the ATM as central arbiter), and **mission execution planning** (Package/Flight with route generation, threat avoidance, and loadout selection). The 200ms time budget, priority queue, and deconfliction system together ensure the AI generates a realistic mix of missions without overwhelming any single area, while the A* pathfinder with pre-computed threat maps provides performant threat-avoiding routing.
The breakdown above covers the full mission generation pipeline from request creation through package finalization. If you'd like, I can dive deeper into any specific subsystem (e.g., the A* pathfinder's cost heuristics, the weapon scoring formula, or the ground tasking manager's objective assignment), or I can save this analysis as a PDF/docx document for reference.

Ideas for improvement
Great question — the Falcon 4 campaign code is a textbook case of 1990s C++ with pervasive `switch`-on-enum, flat static tables, and manually-dispatched message types. Here's how I'd approach a modernization, organized by the major subsystems:

---

## 1. Mission Types: Enum-Table → Polymorphic Hierarchy

**Current state:** `MissionData[]` — a 41-entry static array indexed by `MissionType` enum, with ~20 fields per entry. Every consumer does `switch(missionType)` to vary behavior.

**Modern approach — `std::variant` + visitor for the data, polymorphic hierarchy for behavior:**

```cpp
// Instead of one monolithic struct + enum:
struct MissionData {
    MissionType  type;
    int          priority;
    int          maxPackages;
    uchar        flags;
    // ... 15 more fields, many only meaningful for certain types
};

// Use a tagged variant for the configuration payload:
struct StrikeConfig {
    float minAlt, maxAlt;
    int   requiredHardpoints;
    bool  requiresSEAD;
};

struct CAPConfig {
    float patrolRadius;
    int   stationTime;  // minutes
    float minSpeed;
};

struct AirliftConfig {
    int cargoCapacity;
    bool requiresEscort;
};

using MissionConfig = std::variant<
    StrikeConfig, CAPConfig, AirliftConfig,
    SEADConfig, ReconConfig, AWACSConfig
    // ... per-type configs
>;

// Thin polymorphic base for behavior that varies per mission type:
class MissionBehavior {
public:
    virtual ~MissionBehavior() = default;

    // Each mission type overrides what it needs
    virtual PackagePtr buildPackage(
        const MissionRequest& req,
        const AirbasePool& bases,
        const ThreatMap& threat
    ) = 0;

    virtual std::vector<Waypoint> buildFlightPlan(
        const CampaignPoint& origin,
        const CampaignPoint& target,
        const ThreatMap& threat
    ) = 0;

    virtual LoadoutProfile selectLoadout(
        const AircraftType& ac,
        const ThreatEnvironment& threat
    ) = 0;

    virtual int scoreSquadronFit(
        const Squadron& sqn,
        const MissionRequest& req
    ) const = 0;

    // Factory — replaces MissionData[] lookup
    static std::unique_ptr<MissionBehavior> create(MissionType type);
};
```

This eliminates the 20-field struct where only 5-6 fields are meaningful per type, and every `switch(missionType)` in `BuildPackage()`, `LoadWeapons()`, `SetWPTimes()` etc. collapses into a virtual dispatch.

**For the 41 static entries**, registration replaces the table:

```cpp
// mission_registry.cpp
static const std::unordered_map<MissionType, std::function<std::unique_ptr<MissionBehavior>()>>
    registry = {
    {AMIS_OCA_STRIKE,     []{ return std::make_unique<OCAStrikeBehavior>(); }},
    {AMIS_BARCAP,         []{ return std::make_unique<BARCAPBehavior>(); }},
    {AMIS_SEAD_STRIKE,    []{ return std::make_unique<SEADBehavior>(); }},
    // ... one line per type, extensible without touching a header
};
```

---

## 2. Requests & Messages: Tagged Type Erasure

**Current state:** Requests are `CampRequest` structs with a `RequestType` enum; routing is a chain of `switch` statements in `ProcessRequest()`. The VU message system uses integer message IDs with manual dispatch.

**Modern approach — `std::any` with `typeid` tag dispatch, or a lightweight envelope pattern:**

```cpp
// Tagged message envelope:
class CampaignMessage {
public:
    using Timestamp = std::chrono::steady_clock::time_point;

    template<typename Payload>
    CampaignMessage(Payload&& payload, Priority prio = Priority::Normal)
        : payload_(std::forward<Payload>(payload))
        , typeInfo_(&typeid(Payload))
        , priority_(prio)
        , timestamp_(std::chrono::steady_clock::now())
    {}

    template<typename Payload>
    const Payload* get() const {
        if (*typeInfo_ == typeid(Payload))
            return std::any_cast<Payload>(&payload_);
        return nullptr;
    }

    bool is(const std::type_info& ti) const { return *typeInfo_ == ti; }
    Priority priority() const { return priority_; }
    Timestamp when() const { return timestamp_; }

private:
    std::any            payload_;
    const std::type_info* typeInfo_;
    Priority            priority_;
    Timestamp           timestamp_;
};

// Concrete request types — each is a proper struct, not an enum case:
struct OCARequest {
    ObjectiveID target;
    int priority;
    std::chrono::minutes deadline;
    bool requiresFollowUp;
};

struct BARCAPRequest {
    Waypoint stationPoint;
    float radius;
    int minAircraft;
    std::chrono::minutes onStationTime;
};

struct AirliftRequest {
    UnitID cargoUnit;
    CampaignPoint dest;
    int urgency; // 0-100
};

// Router with double-dispatch or visitor pattern:
class RequestRouter {
public:
    using Handler = std::function<void(const CampaignMessage&)>;

    template<typename PayloadType>
    void on(std::function<void(const PayloadType&)> handler) {
        handlers_[typeid(PayloadType)] = [h = std::move(handler)](const CampaignMessage& msg) {
            if (auto* p = msg.get<PayloadType>())
                h(*p);
        };
    }

    void dispatch(const CampaignMessage& msg) {
        auto it = handlers_.find(*msg.typeInfo_);
        if (it != handlers_.end())
            it->second(msg);
    }

private:
    std::unordered_map<std::type_reference_wrapper, Handler> handlers_;
};
```

This replaces the monolithic `ProcessRequest()` switch with a composable routing table. New request types are added by defining a struct + registering a handler — zero changes to existing dispatch code.

---

## 3. Entity System: VU's Integer Tags → `std::variant` + Component Pattern

**Current state:** VU entities are identified by integer type codes (`VuEntityType`), and every subsystem casts and switches on these. `FalconEntity` is a god-base with dozens of branches.

**Modern approach — variant for the entity type, components for cross-cutting concerns:**

```cpp
// Entity kinds (replaces VuEntityType integer):
enum class EntityKind : uint8_t {
    Aircraft, Squadron, Battalion, Brigade, Division,
    NavalTaskForce, Ship, Objective, Airbase, SAMSite
};

// Entity handle — type-safe, non-copyable, replaces raw VuEntity*:
template<EntityKind K>
class EntityHandle {
public:
    explicit EntityHandle(EntityID id) : id_(id) {}

    // Lazy resolve — only hits the entity table when you dereference
    const auto& operator*() const {
        return EntityTable::resolve<K>(id_);
    }

    EntityID id() const { return id_; }
private:
    EntityID id_;
};

// Components — cross-cutting data that any entity might have:
struct PositionComponent {
    CampaignPoint pos;
    float heading;
    float altitude;
    std::chrono::seconds lastMoved;
};

struct CombatComponent {
    int supply = 100;
    int fatigue = 0;
    int morale = 100;
    float effectiveness() const {
        return (supply / 100.0f)
             * ((100.0f - fatigue) / 100.0f)
             * (morale / 100.0f);
    }
};

struct RadarComponent {
    RadarState state = RadarState::Off;
    float maxRange;
    float currentScanAngle;
    int   radarMode;
};

// Entity itself — kind + optional components:
template<EntityKind K>
struct Entity {
    EntityID id;
    EntityKind kind = K;
    TeamID team;

    // Components are optional — an aircraft might not have a RadarComponent
    // (stealth), a battalion might not have one either
    std::optional<PositionComponent> position;
    std::optional<CombatComponent> combat;
    std::optional<RadarComponent> radar;

    // Kind-specific payload:
    typename EntityPayload<K>::type data;
};

// Payload traits — replaces per-type union in FalconEntity:
template<EntityKind K> struct EntityPayload;
template<> struct EntityPayload<EntityKind::Aircraft> { using type = AircraftData; };
template<> struct EntityPayload<EntityKind::Battalion> { using type = BattalionData; };
template<> struct EntityPayload<EntityKind::NavalTaskForce> { using type = TaskForceData; };
```

The key win here is eliminating the `dynamic_cast<VuAircraft*>(entity)` pattern that permeates the codebase. `EntityHandle<EntityKind::Aircraft>` is statically typed — you can't accidentally pass a battalion to a function expecting an aircraft.

---

## 4. Tasking Managers: Strategy Pattern for the Pipeline

**Current state:** `ATM::Task()` is a ~200-line method with explicit phases (sort → iterate → build → finalize), hardcoded to air tasking. GTM and NTM duplicate the structure with different internals.

**Modern approach — extract the pipeline into a composable strategy:**

```cpp
// Phase interface — each phase is a standalone, testable unit:
class ITaskingPhase {
public:
    virtual ~ITaskingPhase() = default;
    virtual void execute(TaskingContext& ctx) = 0;
};

// Context carries all state through the pipeline:
struct TaskingContext {
    std::vector<CampaignMessage>& requests;
    std::vector<PackagePtr>& packages;
    ThreatMap& threatMap;
    std::chrono::milliseconds timeBudget;
    std::chrono::steady_clock::time_point deadline;
    bool budgetExhausted() const {
        return std::chrono::steady_clock::now() >= deadline;
    }
};

// Concrete phases — composable, reusable:
class SortByPriorityPhase : public ITaskingPhase {
    void execute(TaskingContext& ctx) override {
        std::ranges::sort(ctx.requests, {}, &CampaignMessage::priority);
    }
};

class BudgetAwareIterationPhase : public ITaskingPhase {
    void execute(TaskingContext& ctx) override {
        for (auto& req : ctx.requests) {
            if (ctx.budgetExhausted()) break;
            processSingleRequest(req, ctx);
        }
    }
};

class PackageFinalizationPhase : public ITaskingPhase { /* ... */ };

// Pipeline builder — each manager configures its own sequence:
class TaskingPipeline {
public:
    TaskingPipeline& addPhase(std::unique_ptr<ITaskingPhase> phase) {
        phases_.push_back(std::move(phase));
        return *this;
    }

    void run(TaskingContext& ctx) {
        for (auto& phase : phases_) {
            phase->execute(ctx);
            if (ctx.budgetExhausted()) break;
        }
    }
};

// ATM, GTM, NTM just build different pipelines:
TaskingPipeline ATMPipeline() {
    return TaskingPipeline()
        .addPhase(std::make_unique<SortByPriorityPhase>())
        .addPhase(std::make_unique<CheckNeedRequestsPhase>())  // wait/timer
        .addPhase(std::make_unique<BudgetAwareIterationPhase>())
        .addPhase(std::make_unique<PackageFinalizationPhase>());
}

TaskingPipeline GTMPipeline() {
    return TaskingPipeline()
        .addPhase(std::make_unique<ScoreObjectivesPhase>())
        .addPhase(std::make_unique<CollectUnitsPhase>())
        .addPhase(std::make_unique<MostConstrainedFirstPhase>())
        .addPhase(std::make_unique<AssignmentPhase>());
}
```

This makes the 200ms time budget a first-class concern (not a manual `if (GetTickCount() - start > 200) break;` scattered through every loop), and each phase is independently testable.

---

## 5. Threat Maps & Pathfinding: Templates + Concepts

**Current state:** `SamMapData` and `RadarMapData` are separate 2D arrays with nearly identical access patterns. The A* engine is hardcoded to one map type.

**Modern approach — C++20 concepts for map access, parameterized pathfinder:**

```cpp
// Concept: anything that can be queried for threat at a point
template<typename T>
concept ThreatMap = requires(const T& map, GridPoint pt, float alt) {
    { map.threatAt(pt, alt) } -> std::convertible_to<float>;
    { map.isPassable(pt, alt) } -> std::convertible_to<bool>;
    { map.resolution() } -> std::convertible_to<int>;
};

// Single generic A* that works with any ThreatMap:
template<ThreatMap Map>
class PathFinder {
public:
    PathFinder(const Map& map, const PathFinderConfig& cfg = {})
        : map_(map), cfg_(cfg) {}

    std::optional<std::vector<GridPoint>> findPath(
        GridPoint from, GridPoint to, float altitude
    ) {
        // Same algorithm, but map_.threatAt() and map_.isPassable()
        // are resolved at compile time — no virtual dispatch
        // ...
    }

private:
    const Map& map_;
    PathFinderConfig cfg_;
    // Replace raw array with:
    std::vector<AStarNode> openSet_;  // vector instead of static[2000]
    std::vector<AStarNode> closedSet_;
};

// Usage:
SamThreatMap samMap;
RadarThreatMap radarMap;
PathFinder samPath(samMap);
PathFinder radarPath(radarMap);
// Same code, different maps, zero runtime overhead
```

This collapses the duplicated map logic and makes adding new threat types (e.g., a combined threat map) a zero-effort operation.

---

## 6. Squadron/Aircraft Pool: RAII + Smart Pointers

**Current state:** `FindAvailableAircraft()` returns raw pointers into a global array. Aircraft allocation is manual (set flags, decrement counters). Memory leaks are endemic in the original codebase.

```cpp
// Modern: scoped handles that release on destruction
class AircraftLease {
public:
    AircraftLease(Squadron& sqn, int count)
        : sqn_(sqn), count_(count)
    {
        sqn_.reserveAircraft(count_);
    }

    ~AircraftLease() {
        if (count_ > 0)
            sqn_.releaseAircraft(count_);
    }

    // Non-copyable, movable
    AircraftLease(const AircraftLease&) = delete;
    AircraftLease& operator=(const AircraftLease&) = delete;
    AircraftLease(AircraftLease&& other) noexcept
        : sqn_(other.sqn_), count_(std::exchange(other.count_, 0)) {}

    int count() const { return count_; }

private:
    Squadron& sqn_;
    int count_;
};

// Usage in package building — if an exception or budget timeout
// interrupts the pipeline, aircraft are automatically returned:
void BuildPackage(TaskingContext& ctx) {
    auto lease = squadron.leaseAircraft(requiredCount);
    // ... if anything throws or returns early, lease destructor
    // returns the aircraft to the squadron pool
}
```

---

## Summary of Key Principles Applied

| Old Pattern | Modern Replacement | Benefit |
|---|---|---|
| `MissionData[]` + `switch(MissionType)` | Polymorphic `MissionBehavior` + `std::variant` config | Open/closed, type-safe payloads |
| `RequestType` enum + monolithic `ProcessRequest()` | Tagged `CampaignMessage` + registered handlers | Zero-touch extensibility |
| `VuEntity*` + `dynamic_cast` | `EntityHandle<Kind>` template + component model | Compile-time type safety |
| Monolithic `ATM::Task()` with inline budget checks | Composable `TaskingPipeline` with `ITaskingPhase` | Testable phases, reusable budget logic |
| Duplicate `SamMapData`/`RadarThreatMap` code | `ThreatMap` concept + generic `PathFinder<Map>` | One algorithm, many maps |
| Manual `reserve`/`release` aircraft | RAII `AircraftLease` | Exception-safe, leak-free |

The biggest architectural shift is **replacing the "big switch" pattern with polymorphism/variants everywhere** — which is exactly what modern C++ (17/20/23) is designed to support cleanly with `std::variant`, `std::visit`, concepts, and move semantics. The original code's enum-centric design was a pragmatic choice for 1997-era compilers, but every one of those switch statements is now a friction point for maintenance and extension.